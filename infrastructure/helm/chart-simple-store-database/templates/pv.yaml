apiVersion: v1
kind: PersistentVolume
metadata:
  name: small-store-db-pv-volume
  namespace: staging
spec:
  storageClassName: manual
  capacity:
    storage: 4Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"