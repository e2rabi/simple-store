apiVersion: v1
kind: Service
metadata:
  name: product-composite-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: product-composite-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080