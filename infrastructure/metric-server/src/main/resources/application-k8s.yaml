spring:
  application:
    name: metric-server

server:
  port: 8889

logging:
  level:
    root: info

app:
  eureka:
    username: admin
    password: admin
eureka:
  client:
    serviceUrl:
      defaultZone: "http://${app.eureka.username}:${app.eureka.password}@eureka-service:8761/eureka/"